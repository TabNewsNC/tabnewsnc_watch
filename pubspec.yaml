name: tabnews
description: "Cliente para visualizar e interagir com as notícias/artigos publicados no TabNews"

publish_to: "none"
homepage: https://softyes.com.br
repository: https://github.com/TabNewsNC/tabnewsnc_watch

version: 0.1.0+1

environment:
  sdk: ^3.10.3
  flutter: ^3.38.4

dependencies:
  flutter:
    sdk: flutter

  wear_plus: ^1.2.4
  flutter_modular: ^6.4.1
  rotary_scrollbar: ^1.1.0
  tabnewsnc_ui:
    #version: ^0.1.1
    git:
      ref: 680f17e
      #tag_pattern: v{{version}}
      url: git@github.com:TabNewsNC/tabnewsnc_ui.git
  tabnewsnc_engine:
    version: ^0.1.0
    git:
      tag_pattern: v{{version}}
      url: git@github.com:TabNewsNC/tabnewsnc_engine.git

dev_dependencies:
  flutter_test:
    sdk: flutter

  icons_launcher: ^3.0.3
  very_good_analysis: ^10.0.0
  flutter_native_splash: ^2.4.7
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
